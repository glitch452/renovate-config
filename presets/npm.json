{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",

  "extends": ["npm:unpublishSafe"],

  "packageRules": [
    {
      "description": "Pin all except peer dependencies for npm",
      "extends": [":pinAllExceptPeerDependencies"],
      "matchManagers": ["npm"]
    }
  ]
}
